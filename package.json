{
  "name": "braintrust-token",
  "version": "1.0.0",
  "description": "BTRUST token for the Braintrust marketplace",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^2.3.0"
  }
}
